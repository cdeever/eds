# Terraform state is NOT committed: it holds every credential the substrate
# issued this tenant (ADR-0015 §4).
terraform.tfstate
terraform.tfstate.backup
.terraform/
*.tfplan
.terraform.tfstate.lock.info

# Issued at admission, never committed.
site-ca.pem
