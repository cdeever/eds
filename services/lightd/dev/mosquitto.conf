# Development broker only. mqtt01 on the substrate's IoT Backend segment (VLAN
# 35) requires TLS and credentials; this exists so firmware and lightd can be
# brought up on a workstation before that host is built.
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_type warning
